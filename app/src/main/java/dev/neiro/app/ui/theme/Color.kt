package dev.neiro.app.ui.theme

import androidx.compose.ui.graphics.Color

// Apple Music-inspired dark defaults — used until album art provides a real palette
val NieroBackground   = Color(0xFF0D0D0F)   // near-black
val NieroSurface      = Color(0xFF1C1C1E)   // iOS dark surface
val NieroSurfaceHigh  = Color(0xFF2C2C2E)   // elevated card
val NieroAccent       = Color(0xFFE5484D)   // warm red — overridden by palette
val NieroTextPrimary  = Color(0xFFFFFFFF)
val NieroTextSecondary = Color(0xFF8E8E93)  // iOS secondary gray

// Material seed colours (used when palette = default)
val PrimaryColor  = Color(0xFFE5484D)
val ErrorColor    = Color(0xFFCF6679)
